"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.parseMessage = exports.getState = void 0;

var _constants = require("./constants");

var getState = function getState(_ref) {
  var error = _ref.error,
      success = _ref.success,
      touched = _ref.touched;
  if (error && touched) return _constants.STATE_ERROR;
  if (success) return _constants.STATE_SUCCESS;
  return _constants.STATE_DEFAULT;
};

exports.getState = getState;

var parseMessage = function parseMessage(message) {
  if (!Array.isArray(message)) {
    return message;
  }

  return message.length > 0 ? message[0] : undefined;
};

exports.parseMessage = parseMessage;